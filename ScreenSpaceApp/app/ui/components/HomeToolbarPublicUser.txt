import React from 'react';
import { Text, Pressable, View } from 'react-native';
import { styles } from '../styles/theme';
import { Button, IconButton } from 'native-base';
import Icon from 'react-native-vector-icons/AntDesign';

export default function ToolbarPublicUser(props:{onPressLeft: any; onPressRight: any; title?:string|undefined}){
    const {onPressLeft, onPressRight, title="ToolbarTitle"} = props;
    
    return(
        <View style={styles.toolbarPublicUser}>
            <Pressable style={styles.leftArrowButtonContainer}>
                <Icon name='arrowleft' size={30} color="white"/>
            </Pressable>
            <Text style={styles.toolbarPublicUserText}>
                {title}
            </Text>
        </View>
    )   
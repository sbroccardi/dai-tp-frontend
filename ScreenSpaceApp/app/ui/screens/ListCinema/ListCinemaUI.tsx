import { VStack } from 'native-base';
import React from 'react';
import ToolbarPrivateUser from '../../components/ToolbarPrivateUser';

const ListCinemaUI = () => {
    return (
        <VStack>
            <ToolbarPrivateUser onPress={undefined} />
        </VStack>
    )
}

export default ListCinemaUI;